{
  "name": "real-spc-web",
  "version": "0.1.0",
  "description": "Smart Pick Consulting — smart-pick.co",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy public --project-name=real-spc-web"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
